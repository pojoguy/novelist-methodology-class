# Geography check

Follow Skill **fiction-travel-edit**. Example fixture routes: Lake North↔South City (Beta stop); Gate↔West Ridge; West Fork↔Collapse with rejected bypass.

Before any travel rewrite: `geography_query` with `place_a` / `place_b`. On `NO_ROUTE_CLAIM`, stop and offer `geography_ingest` (dry run) or hand-authored CANON route JSON.

Requires **novelist-mcp**. Restart MCP after plugin updates if tools are missing.
